[project]
name = "local-tools"
version = "0.1.0"
description = "local python tools"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
